1) Crie a listagem dos jogos no index.html.
   Coloque o comportamento no arquivo listagem.js.
   Use async e await na solução.
   Mostre estrelas no lugar da nota.

👉 Dessa vez, faça a solução com a tag template.

DICAS:
    - Um template tem a propriedade "content", que é um Document Fragment
    - Um fragmento pode ser clonado com cloneNode(true)